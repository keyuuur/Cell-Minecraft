# Optional same-origin proxy target. The browser never needs to know this URL.
APPS_SCRIPT_SUBMISSION_URL=
# Shared only between the Vercel Function and Apps Script. Never expose with a VITE_ prefix.
APPS_SCRIPT_PROXY_KEY=
